@model CertificatePortal.Models.ViewModels.CertificateViewModel
@using Microsoft.Extensions.Options
@inject IOptions<CertificatePortal.Models.AppSettings> Settings
@{
    ViewData["Title"] = "Preview Certificate";
    var s = Settings.Value;
    // Generate QR Code for Web Preview
    var qrUrl = $"https://auca.ac.rw/verify/{Model.Record.StudentID}";
    var qrGenerator = new QRCoder.QRCodeGenerator();
    var qrData = qrGenerator.CreateQrCode(qrUrl, QRCoder.QRCodeGenerator.ECCLevel.Q);
    var qrCode = new QRCoder.PngByteQRCode(qrData);
    var qrBase64 = Convert.ToBase64String(qrCode.GetGraphic(5));
}

<style>
    .preview-container {
        max-width: 900px;
        margin: 0 auto;
    }

    .preview-actions {
        display: flex;
        justify-content: space-between;
        margin-bottom: 2rem;
    }

    .certificate-card {
        background: white;
        padding: 4rem;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        border: 1px solid #ddd;
        font-family: 'Times New Roman', Times, serif;
        color: black;
        position: relative;
        min-height: 1050px;
    }

    .cert-header-table {
        width: 100%;
        border-bottom: 2px solid #000;
        padding-bottom: 1rem;
        margin-bottom: 2rem;
        display: flex;
        align-items: center;
        gap: 1.5rem;
    }

    .cert-logo {
        width: 90px;
        height: auto;
    }

    .cert-header-text {
        flex: 1;
        text-align: center;
    }

    .cert-header-text h2 {
        font-family: 'Times New Roman', serif;
        font-size: 1.6rem;
        margin: 0;
        font-weight: bold;
        color: black;
    }

    .cert-header-text p {
        margin: 2px 0;
        font-size: 0.9rem;
    }

    .dept-line {
        font-style: italic;
        font-size: 1.1rem !important;
        margin-top: 4px !important;
    }

    .cert-date {
        text-align: right;
        margin-bottom: 2rem;
        font-size: 1.1rem;
    }

    .cert-title {
        text-align: center;
        margin: 2.5rem 0;
    }

    .cert-title h1 {
        font-family: 'Times New Roman', serif;
        font-size: 1.8rem;
        text-decoration: underline;
        font-weight: bold;
        color: black;
        text-transform: uppercase;
    }

    .cert-body {
        font-size: 1.15rem;
        line-height: 1.7;
        text-align: justify;
    }

    .student-details {
        margin: 1.5rem 0;
    }

    .student-name {
        font-size: 1.3rem;
        font-weight: bold;
        margin-bottom: 0.5rem;
    }

    .field-label {
        font-weight: bold;
    }

    .cert-footer {
        margin-top: 4rem;
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
    }

    .qr-block {
        text-align: right;
    }

    .qr-image {
        width: 90px;
        height: 90px;
    }

    .signature-block {
        text-align: left;
    }

    .sig-image {
        width: 150px;
        height: auto;
        margin-bottom: -10px;
    }

    .sig-name {
        font-weight: bold;
        margin-top: 10px;
    }

    .btn {
        padding: 0.8rem 1.5rem;
        border-radius: 4px;
        font-weight: 600;
        cursor: pointer;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
    }

    .btn-outline {
        border: 2px solid var(--primary-color);
        color: var(--primary-color);
        background: white;
    }

    .btn-gold {
        background-color: var(--accent-color);
        color: var(--primary-color);
    }

    @@media print {
        header, footer, .preview-actions, .no-print {
            display: none !important;
        }
        body {
            background: white !important;
            margin: 0 !important;
            padding: 0 !important;
        }
        main {
            margin: 0 !important;
            padding: 0 !important;
            max-width: 100% !important;
        }
        .certificate-card {
            box-shadow: none !important;
            border: none !important;
            padding: 2.5cm !important;
            width: 210mm !important;
            height: 297mm !important;
            min-height: 297mm !important;
            margin: 0 auto !important;
            page-break-after: always;
            box-sizing: border-box;
        }
    }
</style>

<div class="preview-container">
    <div class="preview-actions no-print">
        <a href="/" class="btn btn-outline">← Back to Search</a>
        <div>
            <button onclick="window.print()" class="btn btn-outline">Print Preview</button>
            <a asp-action="Download" asp-route-studentId="@Model.Record.StudentID" class="btn btn-outline">⬇ Download DOCX</a>
            <a asp-action="DownloadPdf" asp-route-studentId="@Model.Record.StudentID" class="btn btn-gold">⬇ Download PDF</a>
        </div>
    </div>

    <div class="certificate-card">
        <div class="cert-header-table">
            <img src="~/images/auca-logo.png" class="cert-logo" alt="AUCA Logo" />
            <div class="cert-header-text">
                <h2>Adventist University of Central Africa</h2>
                <p>P.O. Box 2461 Kigali, Rwanda | www.auca.ac.rw | info@auca.ac.rw</p>
                <p class="dept-line">Directorate for Admissions and Academic Records</p>
                <p>Mobile Phone: (+250)724 796 996 / 724 474 805/ 788 473 035</p>
                <p>Email: registrar@auca.ac.rw || juvenal.nsengiyumva@auca.ac.rw</p>
            </div>
        </div>

        <div class="cert-date">
            @Model.CityAndDate
        </div>

        <div class="cert-title">
            <h1>CERTIFICATE OF GOOD STANDING</h1>
        </div>

        <div class="cert-body">
            <p>I, the undersigned, <strong>Eng. Nsengiyumva Juvenal</strong>, Director for Admissions and Academic Records of Adventist University of Central Africa, hereby certify that:</p>

            <div class="student-details">
                <div class="student-name">@Model.Record.StudentName</div>
                <div>Born on @Model.FormattedBirthDate</div>
                <div>has been a regular student of this University, registered under ID No. @Model.Record.StudentID,</div>
                <div>From @Model.Record.StudiedFrom to @Model.Record.StudiedTo.</div>
            </div>

            <p><span class="field-label">Year:</span> @Model.Record.Year</p>
            <p><span class="field-label">Faculty:</span> @Model.Record.Faculty</p>
            <p><span class="field-label">Major:</span> @Model.Record.Major</p>
            <p><span class="field-label">Academic year:</span> @Model.Record.AcademicYear</p>
            <p><span class="field-label">Validity:</span> @Model.Record.AcademicYear</p>

            <p style="margin-top: 2rem; font-style: italic;">
                This certificate is issued for any legal or administrative purpose it may serve
            </p>
        </div>

        <div class="cert-footer">
            <div class="signature-block">
                <img src="~/images/signature.png" class="sig-image" alt="Signature" />
                <div class="sig-name">Eng. Nsengiyumva Juvenal</div>
                <div>Director for Admissions and Academic Records</div>
                <div>Adventist University of Central Africa</div>
            </div>

            <div class="qr-block">
                <img src="data:image/png;base64,@qrBase64" class="qr-image" alt="QR Code" />
                <p style="font-size: 0.8rem; margin-top: 0.5rem;">Scan to verify validity</p>
            </div>
        </div>
    </div>
</div>
